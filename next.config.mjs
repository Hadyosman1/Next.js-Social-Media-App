/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["via.placeholder.com", "storage.googleapis.com"],
    // remotePatterns: ["via.placeholder.com", "storage.googleapis.com"],
  },
};

export default nextConfig;
